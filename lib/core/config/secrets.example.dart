/// Template for `secrets.dart`, which is gitignored. Copy this file to
/// `secrets.dart` in the same directory and fill in a real key — the health
/// assistant chat falls back to its local rule-based engine if this is left
/// blank or the key is invalid.
library;

const String geminiApiKey = '';
